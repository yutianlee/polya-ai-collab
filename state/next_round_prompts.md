# Next Round Prompts

The authoritative graph selects
`SHELL-general-d-high-floor-first-drop-CST`.  The completed first-floor WP2
theorem and the completed (d=3) theorem remain frozen on their audited chains.
The graph has 124 unique obligations and SHA-256
`a5b2489ab8a45a5d58b2b76b27cb42ba3fd6ff3c6b40c7dc9d9832789e9003d4`.

## Prompt A: lead analytic CST proof

Work only in the exact Round 27--37 residual sector.  Put \(d=1-2t/\pi\) and

\[
 E=e_0+e_p,\qquad \Delta=e_0-e_p,
 \qquad E_*=\frac12-\frac{dm}{2p}.
\]

1. Freeze the proved automatic sector \(p\le dm\) or \(E\ge E_*\).  Do not
   spend another round on a global lower-scalar sign.
2. On
   \[
    p>dm,\qquad 0\le E<E_*<\frac12,
   \]
   use the proved quartic payment
   \[
    \mathcal K_4=
    \frac{(\mu^{-1}-K^{-1})((r+p)^2-r^2)}{2\pi}
    +\frac{(\mu^{-3}-K^{-3})((r+p)^4-r^4)}{24\pi},
   \]
   together with
   \[
    \tau=\frac{s-1}{s+1},\qquad
    M_4=\max\{\tau(E+2\lambda),\mathcal K_4\}.
   \]
3. Keep the primary complete-terminal sign
   \[
    \Psi^{L_T}_{4,E}
    =\max\{0,L_T\}+a_pM_4+p(E-E_*)\ge0.
   \]
   Preserve every \(\theta_k\), inverse fraction \(\eta_k\), the exact cap,
   the top interval, the actual \(E\), the paired shelf, both parity grids,
   and the actual discrete owner.
4. Import the proved Round 29 reductions: strict increase on each
   fixed-\(\alpha\) literal terminal cell, strict decrease on each fixed-
   \(Q\), constant-\(K\) inverse/outer-\(B\) wall segment, and the exact
   primary-component bound
   \[
    \Psi^{L_T}_{4,E}>
    \frac{371}{15840}+2\eta_1+2E>0
   \]
   for \((r,p,m,f)=(1,2,2,2)\), \(B=Q=1\), \(2<y_1<3\).  Do not spend
   another round searching those interiors.
5. Import the proved Round 30 first-band reductions.  For
   \(\tan\theta_j-\theta_j=3\pi/[4(q+j)]\),
   \(K_j=(q+j)/\cos\theta_j\), \(j=2,3\), define \(T(q)\),
   \(E_{\min}\), \(\mathcal K_{4,\min}\), and \(d_{\min}\) exactly as in the
   audited note, and retain
   \[
    \mathfrak F(q,r,p,m)=T(q)+a_p\mathcal K_{4,\min}
      +p\left(E_{\min}-\frac12\right)+\frac{m d_{\min}}2.
   \]
   The necessary conditions are
   \[
    G_{K_3}(q-m)-G_q(q-m)\ge\frac74,\qquad
    p>d_{\min}m,\qquad
    E_{\min}<\frac12-\frac{d_{\min}m}{2p}
   \]
   Round 31 proves that the dependent profile
   \(p\mapsto\mathfrak F(q,q-m-p,p,m)\) is strictly convex and that this exact
   necessary domain is empty for every real \(q\ge1000\).  Freeze both facts.
   Round 32 then proves the strict shelf bootstrap
   \(m>(6/5)(\pi/\psi-3)\) for \(q\ge35\), uses \(m_0=1\) below \(35\),
   and certifies the two nontrivial continuous \(L_q\) edges \(>1/100\) for
   all 1994 exact \(q<1000\) grid values; \(p=1\) is analytic.  Freeze the
   resulting closure of the included retained-\(E\) shelf.  Keep the false
   untruncated \(m=1\) values at \(q=79/2\) and \(q=40\) as scope regressions.
6. Import Round 33 on the smooth lower-\(Q\) hard arc, where
   \[
    B=1,\qquad 0<y_1<\alpha<1,
   \]
   there is no inverse/outer-\(B\) intersection.  It proves
   \(d(E-E_*)/d\mu<0\) while the same first-drop/activity cell remains
   feasible, so a hard component reaches \(e_p=0\), an activity wall, or
   \(\alpha\uparrow1\).  It does not prove that the complete scalar is
   monotone.  Import Round 34 next: activity is automatic throughout the
   high-floor first shelf, and lower-\(Q\) hard-sector exclusion reduces to
   \[
    \frac{2P(2z+P)}{\{b(z)+b(z+P)\}D}>
    \frac{P-d(t)M}{2P}
   \]
   under the exact normalized conditions (3.16), followed by the intrinsic
   \(Q\to1^-\) face and its finite stationary alternative.  Import the
   small-phase adjacent-action theorem and Round 35, which close that face for
   all \(t\) and prove the transported domain empty for
   \(t\ge3\pi/14\).  Treat the lower-\(Q\) hard sector as closed.  Import
   Round 36 next: \(B-Q\in\{0,1\}\); outer \(B_N\) precedes \(Q_N\) for
   \(\alpha>0\); and on \(B=Q+1\),
   \[
    L_T>1-J>\frac67,
    \qquad
    \Psi^{L_T}_{4,E}>\frac67-\frac{p-dm}{2}.
   \]
   Freeze the closure \(p-dm\le12/7\).  Import Round 37 next: on the hard
   gap,
   \[
    B_0=Q=B-1\ge1,
    \qquad j=f-B=\lfloor\lambda\rfloor,
   \]
   the top interval vanishes, and the selected scalar has the lossless form
   \[
    \Gamma_{\rm gap}
    =1-J+\frac{B_0d}{2c}+\Omega
     +(p+a_p)M_4+p\Xi-\frac{p-dm}{2},
    \qquad \Omega>0,\quad\Xi\ge0.
   \]
   Round 37 also proves \(\Phi_\delta^+>\mathcal H_\Delta\), with the exact
   elasticity, quartic, and adjacent-action payments combined by one maximum.
   Freeze these reductions but not their signs.  First prove
   \(\Gamma_{\rm gap}>0\) or sufficiently \(\mathcal H_\Delta\ge0\) on the
   transported outer, inverse, lower-shelf, and alpha-endpoint faces with
   \(p-dm>12/7\), without splitting by \(B\) or \(j\).  Then treat higher
   \(Q_N\) and equal-count inverse faces, exceptional
   \(\alpha=0\) simultaneous \(B/Q\) corners, and general lower shelves
   outside \(e_p=0,\ B=Q=1,\ 2<y_1<3\).  The pure \(E=E_*\) seam is already strict because
   \(a_p\mathcal K_4>0\).  Do not assume global monotonicity in \(\alpha\).
7. Reproduce the Round 22 and Round 26--33 regression cases.  In particular,
   the Round 27 tuple must be classified as automatic-sector with
   \(\mathcal R_J<-6/5\), \(\mathcal C_{\max,8}<-4/3\),
   \(\mathscr S>47\), and \(\Phi_\delta>40\).  The Round 29 primary margin,
   adaptive cusp, wall jumps, inherited-grid restriction, and the Round 30
   activity, shelf, lower-\(Q\), \(H(t)\), and retained-\(E\) fixtures are
   mandatory regressions.  Also replay the Round 31 exact convexity/Hessian
   identities, the \(q\ge1000\) phase margins and nine bootstrap implications,
   and the \((q,m,p,r)=(100000,150,611,99239)\) negative-\(L_q\) witness.  The
   last point has \(E_{\min}>1.7\), lies outside the hard sector, and is not a
   counterexample to \(\mathfrak F\), CST, or the theorem.  Replay the Round
   32 five exact margins, 1994-value/3988-edge directed certificate, floor
   transitions, \(q=999.5/1000\) seam, and negative untruncated witnesses.
   Replay the Round 33 wall factorization and nine zero Mathematica residuals;
   reject the false lower bracket \(3/4-t/\pi<W\).
   Replay the Round 34 activity reserve, adjacent-action derivative, Jensen
   second moment, quadrature primitives, and normalized scale identity.  Keep
   the observed \(0.014074\) scalar margin diagnostic-only.  Replay the
   small-phase Bernstein identity and both Round 35 exact symbolic files;
   verify the corrected \(D_1<21/20\) residual denominator and the
   stationary discriminant factor independently.  Replay
   `computations/general_d_round36_count_gap_replay.wl` and require every
   strict-bracket/jump check and `round36CountGapReplayOK=True`.
   Replay `computations/general_d_round37_gap_interface_replay.wl` and require
   all 15 exact identities plus `round37GapInterfaceReplayOK=True`.  Run
   `computations/general_d_round37_gap_rootfree_diagnostic.py` only as a
   finite ordinary-double falsification search; no sampled minimum is a
   theorem premise.
8. Keep \(\Psi^{\rm rf}_{4,E}\) and conditional
   \(\mathcal C_{\max,8}\) as auxiliary falsification gates only.  If the
   complete-terminal pointwise route genuinely fails, return to
   \(\mathscr S\) and then activate the weighted aggregate WT rather than
   another surrogate or ratio ladder.

Do not introduce a ratio threshold or another compact certificate.

## Prompt B: clean-room CST reconstruction

Without reading the incumbent proof first, reconstruct S1--S3, T1--T4, the
exact scalar \(\mathscr S\), and the wall-safe scalar
\[
 \Phi_\delta^+
 =\max\{0,L_T\}+a_p\Delta+p(E-\frac12)+\frac{dm}{2}.
\]
Prove independently that this closes \(p\le dm\) or \(E\ge E_*\), and that
negating those alternatives gives exactly the residual sector and inequality
in Prompt A.  Derive both
\(\Delta\ge(s-1)(E+2\lambda)/(s+1)\) directly from the Round 21 elasticity
inequality and the strict quartic curvature gain \(\Delta>\mathcal K_4\)
from the positive binomial expansion of \(\sigma'\).  Reconstruct the chain
\[
 D_A(r)\ge\Phi_\delta^+\ge\Psi^{L_T}_{4,E}
 \ge\Psi^{\rm rf}_{4,E}
\]
without reading the Round 28 note.  Independently prove that
\(H(t)=E(t)-E_*(t)\) is strictly increasing on \(p>d(t)m\), and audit the
automatic/hard seam, ordinary shelf walls, inverse-spatial walls,
maximum-switch wall, top-payment walls, and both extension grids.  Attempt a
clean-room reconstruction of the Round 29 phase derivative, raw wall jumps,
fixed-\(K\) radius-average/elasticity proof, and primary-cell rational margin.
Respect the inherited scopes \(r\ge1\) and \(r\ge3/2\).  Reconstruct the
Round 30 whole-band activity estimate, included-shelf derivative and
\(L_T>7/10\) reserve, every comparison direction in
\(\Psi^{L_T}_{4,E}>\mathfrak F\), the lower-\(Q\) wall geometry, and the
\(\alpha=0\) \(H(t)<1\) closure.  Then reconstruct Round 31 without reading
its incumbent proof: derive the exact kernel for the dependent profile
\(p\mapsto\mathfrak F(q,q-m-p,p,m)\), prove it positive on
\(u,v\ge0\), \(v>0\), \(u+v<1\), derive all \(q\ge1000\) phase and endpoint
bounds, and rebuild the retained-\(E\) intrinsic bootstrap
\[
 1\to\frac{25}{17}\to\frac53\to2\to\frac{25}{11}
 \to\frac52\to3\to4\to10.
\]
Verify that this is an implication chain inside one exact domain, not a
shell-ratio owner partition.  Finally reconstruct the Hessian argument for
the weaker scalar \(L_q\) and its reduction to the three edges, without
claiming an edge sign or a direct edge reduction of \(\mathfrak F\).  Then
reconstruct Round 32: prove \(\theta<219/400\), \(\psi<2/3\),
\(q\psi^3<13\), the phase-shape bound \(h(q-y)<2\psi/3\), and the strict
self-bootstrap \(m>(6/5)y\).  Audit the piecewise boundary \(m_0=1\) below
\(q=35\), the exact integer floor above it, the three boundaries of the
truncated triangle, and the \(q=999.5/1000\) seam.  Independently replay the
single directed certificate; do not replace its continuous root enclosures
by integer \(p\)-samples.

## Prompt C: adversarial curvature-cell referee

Treat global \(\mathcal R_J\ge0\), global
\(\mathcal C_{\max,8}\ge0\), and global \(\alpha\)-monotonicity as already
false.  Treat the Round 29 primary inverse component and the exact Round 30
activity, included-shelf terminal, lower-\(Q\) \(y_1>1\), and
\(\alpha=0\), \(4/5\le t<13/10\) sectors as analytically closed regressions.
Also treat the Round 31 convexity/large-\(q\) result and the Round 32 closure
of the included retained-\(E\) shelf, plus the small-phase/Round 35 closure
of the lower-\(Q\) hard sector and the Round 36 gap subfamily
\(p-dm\le12/7\), as closed.  Treat the Round 37 synchronization,
positive-interface reduction, zero top interval, lossless normal form, and
strict \(\Phi_\delta^+>\mathcal H_\Delta\) bound as proved, but treat both
residual signs as open.  Audit the piecewise
\(m_0\) definition, the exact floor transitions, and the false untruncated
\(m=1\) regressions near \(q=40\); never extend \(L_q\) beyond its certified
truncated finite scope.  Search the endpoint families outside the proved scopes
with exact shelf/activity tests and separate one-sided terminal walls.
Concentrate first on the Round 37 failure patterns (9.2)--(9.4) on gap-wall
endpoints with \(p-dm>12/7\), preserving \(\Omega\), \(\Xi\), \(e_p\),
\(\{\lambda\}\), \(J\), and \(W\).  Then audit the other Round 36 classes:
higher \(Q_N\) faces \(N\ge2\), equal-count inverse faces,
exceptional \(\alpha=0\) simultaneous \(B/Q\) corners, and general lower
shelves/\(\alpha\)-endpoint closures outside Round 32.  Also inspect \(E\downarrow0\),
the elasticity/curvature switch, \(B_0=1\), \(\lambda\downarrow0\), and
\(\alpha\uparrow1\).  Treat the pure \(E=E_*\) seam as already strict from
\(a_p\mathcal K_4>0\); inspect only its collisions with another literal
endpoint.  Recompute
\(\Psi^{L_T}_{4,E}\), \(\Psi^{\rm rf}_{4,E}\), the exact residual,
\(\mathscr S\), \(\Phi_\delta^+\), and conditional
\(\mathcal C_{\max,8}\) independently.  Search explicitly for a nonpositive
one-sided endpoint value of the full expression, including every
\(\eta_k\) and \(\theta_k\).  Reject any proof that drops inverse fractions,
replaces actual \(E\) by \(M_4\), uses a known false global sign, or treats a
finite diagnostic minimum as continuum coverage.

Do not identify the \(\alpha=0\) combined \(B/Q\) corner with the open
one-level-gap limit, and do not extend Round 32 beyond
\(e_p=0,\ B=Q=1,\ 2<y_1<3\).

## Prompt D: high-floor directed evaluator

First replay the frozen Round 27 directed-Arb counterexample at 256, 512,
and 768 bits and confirm its automatic-sector classification.  Then run the
two published Round 28 commands:

- `computations/general_d_round28_endpoint_curvature_projection_diagnostic.py`
  with `--limit 15 --random 2000 --wall-limit 60 --order 1`;
- `computations/general_d_round28_hard_sector_cmax_branch_diagnostic.py`
  with `--limit 5 --random 200000 --seed 20260728`.

Require the frozen counts 13,400/111,260 and 34,421/23,878, all reported
zero-negative gates, the rejected-scalar automatic assertions, and the
published limiting-face values.  Replay both Mathematica files with
`wolframscript.exe` and require `round28ProjectionReplayOK=True` and
`round28DerivativeReplayOK=True`.

Then run:

- `wolframscript.exe -file computations/general_d_round29_cell_derivatives.wl`;
- `python -B computations/general_d_round29_primary_face_rational_replay.py`;
- `python -B computations/general_d_round29_adaptive_cusp_diagnostic.py`.

Require twelve zero derivative/wall residuals, the exact margin
\(371/15840\), and both Round 29 replay gates.  Report software versions,
commands, wall conventions, counts, and current hashes.  Classify both finite
Round 28 sweeps and the Round 29 cusp as `floating_point_experiment`; the
Round 29 Fraction and Mathematica scripts replay printed analytic identities
but do not replace their proofs.

Finally run:

- `wolframscript.exe -file computations/general_d_round30_endpoint_derivatives.wl`;
- `python -B computations/general_d_round30_retained_shelf_diagnostic.py --limit 300`;
- `python -B computations/general_d_round30_retained_shelf_diagnostic.py --limit 1000`.

Require seventeen zero symbolic residuals, twelve true rational comparisons,
the exact positive shelf-margin fixtures, 933 retained records in both scans,
minimum \(\mathfrak F(5,1,3,1)=0.300903146691255\ldots\), and no retained
record above \(q=33\).  Classify both finite Round 30 scans as
`floating_point_experiment`: they do not prove the observed \(q=33\) cutoff,
certify the minimum, or replace Round 31's separate analytic \(q\ge1000\)
exclusion.  Never promote a finite diagnostic to continuum coverage or a
certificate.

Finally run:

- `wolframscript.exe -file computations/general_d_round31_retained_shelf_replay.wl`.

Require zero convexity and Hessian residuals, six positive exact angle/phase
margins, two positive endpoint-ratio margins, nine positive bootstrap margins,
and `round31RetainedShelfReplayOK=True`.  Record Mathematica 15.0 and
WolframScript 1.14.0, the exact command, and the current source/audit/WL
hashes.  This exact replay supports the audited analytic identities and
rational comparisons; Round 32, not this replay alone, proves the finite
edge signs.

Finally run:

- `wolframscript.exe -file computations/general_d_round32_shelf_bootstrap_replay.wl`;
- `python -B computations/general_d_round32_truncated_Lq_certificate.py`.

Require the five printed positive rational margins, four zero derivative
residuals, `round32ShelfBootstrapReplayOK=True`, exactly 1994 \(q\)-values,
3988 nontrivial continuous edges, the analytic \(p=1\) margin \(1/5\), the
certified edge margin \(1/100\), and
`round32TruncatedLqCertificateOK=True`.  Verify python-flint 0.8.0, Arb at
192 bits, the piecewise \(m_0\), exact floor transitions, and the
\(q=999.5/1000\) seam.  This is the one selected compact finite certificate;
do not create another.

Also run:

- `wolframscript.exe -file computations/general_d_round36_count_gap_replay.wl`.

Require every printed check to be `True` and the final gate
`round36CountGapReplayOK=True`.  This is an exact symbolic replay of the
Round 36 analytic proof, not a new interval certificate.

Also run:

- `wolframscript.exe -file computations/general_d_round37_gap_interface_replay.wl`;
- `python -B computations/general_d_round37_gap_rootfree_diagnostic.py --limit 10 --random 500 --wall-limit 50`.

Require all 15 Mathematica identities and
`round37GapInterfaceReplayOK=True`.  The Python output must identify itself as
ordinary-double, finite-owner/wall sampling with
`scanCertification=diagnostic_only`; it is a regression and counterexample
search, not a sign certificate.

## Prompt E: d=3 clean-room mathematical reconstruction

Work only from the theorem statement, definitions, parameter ranges, and
allowed structural lemmas. Reconstruct the ratio-sharp global closure without
reading the incumbent derivation first. Check:

- strict layer and half-integer walls;
- \(q\leq\arccos(\rho)/\pi\) and the disjoint angular blocks;
- the global tangent-envelope minorant;
- Stieltjes endpoint signs and the \(\tau>1/4\) scope;
- nonnegativity before the ball-inverse squaring step;
- every exact rational constant and convexity implication; and
- the limiting regimes \(\rho\downarrow0\), \(\rho\uparrow39/50\), and
  \(K\downarrow\pi/(1-\rho)\).

Return the first unsupported implication, or an explicit PASS with a complete
dependency list.

## Prompt F: d=3 adversarial referee

Assume the ratio-sharp theorem is false. Reconstruct every displayed identity,
reverse-check every inequality direction, and search for a counterexample at
turning points, splice points, common walls, and parameter seams. Verify that
the final owner cover is disjoint and exhaustive. Do not rely on numerical
sampling as proof.

## Prompt G: d=3 manuscript referee

Review the 23-page main paper and 9-page analytic support file as a submission.
Check theorem scope, definitions, citation completeness, notation consistency,
all cross-references, and whether the support file really contains every
nonstandard estimate invoked by the main paper. Separate mathematical errors
from exposition requests.

## Prompt H: literature and novelty review

Search current primary literature for sharp Dirichlet Pólya inequalities on
three-dimensional spherical shells, annuli, radial domains, Bessel
cross-product spectra, and related non-tiling domains. Compare exact theorem
scope and constants. Do not infer novelty from absence in a limited search.

## Freeze rules

- Do not reintroduce \(\rho_*\), \(k_6,\ldots,k_{11}\), the 38-state theorem,
  residual sets, or finite ledgers as live premises.
- Preserve the no-mode face and the optical ownership of \(\rho=39/50\).
- Keep computation classified as regression unless a distinct
  computer-assisted theorem is explicitly proposed.
- Do not broaden the result beyond the spherical-shell class.
- Do not transfer the completed (d=3) status to the general-dimensional
  manuscript.
- Do not introduce another general-(d) ratio ladder.
- Do not rewrite the general-dimensional manuscript until the standalone CST
  note closes a gate.
- Keep the Round 21 coarse sign \(\mathcal R\ge0\) in the rejected-claim
  ledger.  Retain the exact cap in \(\mathcal R_J\) to the audited Round 23
  final comparison.
- Preserve Round 24's scope: the \(t\)- and \(p\)-candidate rules are coupled,
  \(\widehat{\mathcal C}_8\) is restricted to the delicate quadratic cell,
  and neither global \(\alpha\)-monotonicity nor lower-wall dominance is a
  proved lemma.
- Preserve Round 25's scope: \(\mathcal C_8\ge\mathcal F\) is an interior
  delicate-cell reduction.  The endpoint-window relaxation is rejected, and
  Round 26 further rejects \(\mathcal F\ge0\) on the full coupled-owner image;
  neither failure is a CST counterexample.
- Keep both Round 26 rejections in the permanent ledger:
  \(\mathcal F\ge0\) fails at the coupled \(p=6\) witness, and
  \(\mathcal C_8\ge0\) fails at the exact/stationary \(p=80\) witness.  The
  latter has \(\mathcal R_J>20\), so it requires restoration of the elasticity
  and cap reserves, not abandonment of CST.
- Keep the Round 27 rejection in the permanent ledger: global
  \(\mathcal C_{\max,8}\ge0\) and global \(\mathcal R_J\ge0\) fail at the
  certified \(p=32\) integer-grid witness.  The same point has
  \(\mathscr S>47\), \(\Phi_\delta>40\), and \(E>1>E_*\), so it is an
  automatic-sector example and not a CST counterexample.
- Use \(\mathcal R_J\) only for the exact-cap repaired intrinsic lower scalar;
  use \(\mathscr S\) for the exact reduced scalar.  Do not conflate them.
- Freeze the automatic sector \(p\le dm\) or \(E\ge E_*\).  Use
  \(\mathcal C_{\max,8}\) only as a conditional sufficient target on the
  complementary exact hard sector; it contains no \(J\) and is not equivalent
  to the exact residual.
- Preserve Round 28's strict quartic payment and actual-\(E\) ordering:
  \[
   \Phi_\delta^+\ge\Psi^{L_T}_{4,E}\ge\Psi^{\rm rf}_{4,E}.
  \]
  The complete-terminal scalar is primary; never discard \(\eta_k\), \(J\),
  the top interval, or \(E\) before testing its surviving endpoint sign.
- Preserve the one-hard-interval and finite Cmax candidate lemmas, but do not
  promote their candidate signs.  Conditional Cmax remains auxiliary, and
  its finite search is diagnostic rather than a global minimization.
- Preserve Round 29's strict literal-cell and constant-\(K\) wall
  monotonicities and the primary inverse-cell margin.  Do not return those
  interiors to the candidate list.
- Preserve Round 30's exact scope for its terminal and shelf statements:
  the \(L_T>7/10\) and retained-\(E\) reductions
  only on the included lower shelf; the lower-\(Q\) exclusion only for
  \(y_1>1\); and the \(H(t)<1\) closure only for \(\alpha=0\), \(f=2\),
  \(4/5\le t<13/10\).  Full-\(\Psi\) shelf or Q monotonicity was not proved.
- Preserve Round 31's exact scope: convexity is for
  \(p\mapsto\mathfrak F(q,q-m-p,p,m)\) at fixed \((q,m)\); the necessary
  shelf/hard domain is empty only for real \(q\ge1000\); and only the weaker
  finite scalar \(L_q\) reduces to the three edges.  Round 31 alone proves no
  finite edge sign.
- Preserve Round 32's exact scope: \(m>(6/5)(\pi/\psi-3)\) only for
  \(q\ge35\), while \(m_0=1\) below \(35\); the single certificate covers
  exactly the 1994 inherited grid values \(3\le q<1000\) and the two
  nontrivial continuous edges of the truncated triangle.  It closes only the
  Round 30 included retained-\(E\) shelf, not another endpoint family.
- Preserve Round 33's exact scope: on the open lower-\(Q\) wall and only while
  the same first-drop/activity cell remains feasible,
  \(d(E-E_*)/d\mu<0\).  A hypothetical hard component is localized to
  \(e_p=0\), an activity wall, or \(\alpha\to1^-\).  Round 33 proves neither
  full-\(\Psi\) monotonicity nor positivity at any of those exits.  Use only
  \(W<3/4\); the tempting lower bracket \(3/4-t/\pi<W\) is false.
- Preserve Rounds 34--35's exact scope: activity is automatic on the full
  high-floor first shelf, and the normalized inequality (3.15) is a sufficient
  route whose face and stationary alternatives are now closed.  This proves
  only the lower-\(Q\) hard sector, not high-floor CST.  The Jensen and
  trapezoid bounds must be combined before eliminating \(\mu\); observed
  continuous margins are not theorem constants.
- Preserve Round 36's exact scope: \(B-Q\in\{0,1\}\), the gap reserve is
  \(L_T>1-J>6/7\), and only \(p-dm\le12/7\) is closed in full.  Higher
  \(Q_N\) terminal positivity is not a full-\(\Psi\) sign.  Keep the
  \(\alpha=0\) simultaneous \(B/Q\) corner distinct from the
  \(\alpha\downarrow0\) one-level-gap limit, and keep Round 32 restricted to
  \(e_p=0,\ B=Q=1,\ 2<y_1<3\).
- Preserve Round 37's exact scope: \(B_0=Q=B-1\ge1\), the top interval is
  zero, \(\Gamma_{\rm gap}\) is a lossless normal form, and
  \(\Phi_\delta^+>\mathcal H_\Delta\) is only a sufficient reduction.
  Neither residual sign, CST, nor the theorem is proved.  Next attack one of
  these two exact residual signs on \(p-dm>12/7\), retaining \(\Omega\),
  \(\Xi\), \(e_p\), \(\{\lambda\}\), \(J\), and \(W\).  Do not split by
  \(B\) or \(j\).  If it resists, move to the exact shifted-tail
  scalar and then the weighted aggregate before considering any certificate.
- Keep the Round 30 \(q\le1000\) scan, its 933 retained records, sampled
  positive minimum, and observed \(q=33\) cutoff diagnostic-only.  None is an
  exact cutoff theorem or finite certificate.  Keep the unarchived Round 31
  edge experiment non-load-bearing as well.  The Round 32 directed-Arb
  artifact, not those experiments, is the certified finite dependency.
- Keep the constant-\(K\) hard-gap proof on the inherited extension grids:
  \(r\ge1\) on the integer grid and \(r\ge3/2\) on the half-integer grid.
  Its auxiliary inequality is not proved at \(r=0\) or \(r=1/2\).
- Treat the adaptive cusp only as diagnostic evidence against global
  \(\alpha\)-monotonicity.  Its positive decimal value is not a continuum
  premise.
- Return first to the hard-sector residual, then to \(\mathscr S\) or the
  weighted aggregate.  Do not propose another compact certificate; before
  final assembly, analytically replace the legacy first-floor check or fold
  it into the same consolidated verification artifact as Round 32.
