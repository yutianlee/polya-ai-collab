# Gap Register

The authoritative statuses are in `state/proof_obligations.yml`.

## Active shell gaps

- Complete the separated shell spectrum and strict count bridge (`SHELL-sturm-liouville-completeness` and `SHELL-cross-product-formula`).
- Combine the explicit fixed-$\rho$ threshold $K_0(\rho)$ with compact-$\rho$ and endpoint estimates. In particular, $K_0(\rho)$ diverges as $\rho\to1$ and does not by itself bound the residual global parameter region.
- Build certified verification for the remaining shell cross-product roots, phase counts, and lattice walls after the endpoint analysis produces a bounded covering.

## Closed shell prerequisites

- The global half-integer Bessel cross-product phase enclosure.
- The exact weighted Weyl integral and multiplicity-to-shifted-tail scaffold.
- Every shifted tail starting at or above the inner interface.
- Every shifted tail starting below the inner interface for fixed $\rho$ and $K\ge K_0(\rho)$, with explicit $K_0$.
- The complete fixed-$\rho$ high-energy multiplicity-weighted phase proxy.
- The phase-level zero-count lemma for $(1-\rho)K\le\pi$.

## Parallel-track gaps

- Mathieu-function source audit for the ellipse track.
- Jiang–Lin source audit for the certificate-family fallback.
