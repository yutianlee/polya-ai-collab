# Lemma Bank

The authoritative statements and evidence are in `state/proof_obligations.yml`.

Validated shell lemmas now include:

- exact monotone shell phase representation and global phase enclosure;
- exact strict phase-level counting convention;
- exact weighted Weyl integral;
- $2\ell+1$ multiplicity-to-shifted-tail reduction;
- high-angular shifted-tail bounds for starts at or above $\rho K$;
- fixed-$\rho$ high-energy shifted-tail bounds for starts below $\rho K$, with explicit $K_0(\rho)$;
- the complete fixed-$\rho$ high-energy weighted coarse, strict, and optimized phase proxies;
- $\theta'_{\ell+1/2}\le1$ and zero strict radial phase count when $(1-\rho)K\le\pi$.

The weighted packet is discharged at fixed $\rho$ and high energy. The next primary lemma packet should close `SHELL-sturm-liouville-completeness`; the next analytic integration packet should address `SHELL-rho-uniformity` using the explicit $K_0(\rho)$ and the exact thin-width zero region.
