[agents]
# Requested project cap for future Codex sessions.  The active product/runtime
# may impose a lower hard limit; this setting cannot override that limit.
max_threads = 8
max_depth = 1
interrupt_message = true
