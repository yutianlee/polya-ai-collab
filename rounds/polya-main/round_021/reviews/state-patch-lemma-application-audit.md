# Round 21 Lemma State Patch Application Audit

Date: 2026-07-15

## Verdict

**PASS. First issue: none.**

The working-tree `state/proof_obligations.yml` is the exact canonical result
of applying the final Round 21 lemma judge once to the committed 57-node
graph, modulo only the application timestamp generated by the patch engine.
The realized operation count, statuses, edges, evidence, hashes, provenance,
legacy diagnostic detachments, and nonpromotion decisions all match the
pre-application audit.

The applied graph itself has SHA-256

`a7f8c093f42522465862ea28bf57b1ee60be8b7f16804cebb300b0924ac7d224`.

This audit does not modify the graph or reapply the patch.

## Authenticated baseline and authority

Before inspecting graph content, I reauthenticated:

- final judge
  `rounds/polya-main/round_021/judge/judge-021-lemma.md`:
  `a620175fc43591cd80fcc9f50165d7b21596b077d92fbc4450167d21a4eca9aa`;
- independent pre-application State Patch audit
  `rounds/polya-main/round_021/reviews/state-patch-lemma-audit-source-exec-final.md`:
  `bcd67c3939892c4014026c0ebc5103033bdd33dc68f67082f7bd32ef8c0d84fa`.

`HEAD` contains the committed pre-application graph and has 57 obligations.
Before this report was created, the only working-tree change was
`state/proof_obligations.yml`; no evidence, judge, computation, or test byte
had changed since the pre-application audit.

## Canonical serialization and exact expected content

The applied file parses as one graph mapping and is already in the exact
canonical serialization produced by `math_collab.proof_obligations.dump_graph`:
two-space JSON indentation, ASCII escaping, preserved mapping/list order,
and one terminal newline.

I independently performed the following read-only comparison:

1. loaded `HEAD:state/proof_obligations.yml` as the 57-node baseline;
2. extracted the State Patch from the authenticated final judge;
3. applied it in memory with `round_index=21` and judge reference
   `rounds/polya-main/round_021/judge/judge-021-lemma.md`;
4. substituted the actual application timestamp only in the 3 created and
   13 updated nodes; and
5. compared the complete expected mapping with the working-tree graph.

The result was exact equality. There is no residual field, list ordering,
evidence entry, metadata value, top-level value, or serialization difference
after that timestamp substitution.

The one realized timestamp is

`2026-07-15T08:48:43`.

All 16 affected nodes carry exactly that value and
`last_updated_round: 21`. No unrelated existing node differs from `HEAD`.

## Realized operation result

The actual and independently reconstructed results are identical:

- obligations: 57 before, 60 after;
- created: 3;
- updated: 13;
- rejected: 0; and
- no-change bookkeeping: 11.

The three created ids occur exactly once each:

1. `CERT-round21-compact-proxy`;
2. `CERT-round21-aggregate-tail`; and
3. `SHELL-exact-d20-closure`.

The 13 updated ids are exactly:

- `CONV-strict-counting`;
- `SHELL-sturm-liouville-completeness`;
- `SHELL-phase-enclosures`;
- `SHELL-weighted-lattice-fractional`;
- `SHELL-low-interface-fixed-rho-high-energy`;
- `SRC-annuli`;
- `SHELL-combined-closure`;
- `SHELL-rho-compact-analytic-envelope`;
- `SHELL-rho-compact`;
- `SHELL-rho-uniformity`;
- `TARGET-shell-d3`;
- `POLYA-program-target`; and
- `COMP-certified-bessel`.

The top-level rejected-claim data is unchanged. Every declared no-change
obligation retains its intended theorem/status decision.

## Graph validator, cycles, references, and reciprocity

The canonical graph validator reports:

```text
Graph OK: state/proof_obligations.yml
```

The realized graph has:

- no dependency cycle;
- no implication cycle;
- no dangling dependency;
- no dangling permitted dependency;
- no dangling implication;
- no dangling blocker; and
- no missing direct/reverse edge involving any created obligation.

The two certified computation nodes imply `SHELL-exact-d20-closure`, which
depends on both. The exact-D20 lemma implies
`SHELL-rho-compact-analytic-envelope`, which depends on it. All reverse
foundation implications and forward proof dependencies agree.

Across the full applied graph, all 994 evidence and review-artifact path
occurrences, representing 276 unique paths, resolve to existing files.

## Exact status and edge delta

The only new statuses are those of the created nodes:

| created obligation | actual status |
| --- | --- |
| `CERT-round21-compact-proxy` | `certified` |
| `CERT-round21-aggregate-tail` | `certified` |
| `SHELL-exact-d20-closure` | `proved_internal` |

No pre-existing status changes. In particular:

- `SHELL-rho-compact-analytic-envelope` remains `proved_internal`;
- `SHELL-rho-compact` remains `open`;
- `SHELL-rho-uniformity` remains `open`;
- `TARGET-shell-d3` remains `open`;
- `POLYA-program-target` remains `open`;
- `COMP-certified-bessel` remains `diagnostic_only`; and
- both Bessel pilot nodes remain `certified` regression boxes.

The realized list-edge totals are exactly:

| field | `HEAD` | applied | net |
| --- | ---: | ---: | ---: |
| `dependencies` | 201 | 219 | +18 |
| `permitted_dependencies` | 131 | 150 | +19 |
| `implies` | 109 | 128 | +19 |
| `blockers` | 11 | 8 | -3 |

This is the audited intended delta: 19 new-node dependencies plus the
analytic-envelope dependency and two diagnostic dependency removals; 19
new-node permitted dependencies plus the envelope addition and theorem-target
removal; three new-node implications plus 17 reverse foundation implications
and one obsolete diagnostic implication removal; and the three declared
diagnostic blocker removals.

The attempted permitted-dependency removal from `SHELL-rho-compact` remains
a safe no-op because that list did not contain the diagnostic parent before
application. No undeclared edge was removed.

## Application-added judge evidence and Round 21 provenance

The patch engine correctly added the authenticated judge path to the
`inconclusive` evidence bucket of each created node:

`rounds/polya-main/round_021/judge/judge-021-lemma.md`.

It occurs exactly once in each of the three new nodes and nowhere else in
the graph. This is the expected application-mechanism provenance entry; it
does not relabel the judge itself as a certificate or analytic proof.

The actual evidence counts for the new nodes are:

| obligation | positive | negative | inconclusive |
| --- | ---: | ---: | ---: |
| `CERT-round21-compact-proxy` | 11 | 9 | 1 |
| `CERT-round21-aggregate-tail` | 12 | 9 | 1 |
| `SHELL-exact-d20-closure` | 18 | 16 | 1 |

The sole inconclusive entry in each row is the application-added judge
reference. Every one of the 13 updated nodes also carries Round 21 and the
same application timestamp, but the engine correctly does not inject the
judge path into their evidence buckets.

No changed node contains a duplicate dependency, permitted dependency,
implication, blocker, falsification case, clean-room artifact, adversarial
artifact, certification artifact, or evidence entry. Together with the
single occurrence of every created id and exact expected-graph equality,
this excludes a repeated or partially repeated application.

## Current hashes and evidence classification

The applied graph preserves exactly the authenticated hash fields from the
final judge:

- 63 `artifact_hashes` occurrences;
- 62 raw-file hash occurrences over 41 distinct files; and
- one canonical compact certificate-content digest.

The referenced current bytes remain unchanged from the pre-application
audit, and all raw hashes still match. The current positive source-execution
chain remains:

- scoped candidate `d8cf6427...`;
- wrapper `31130de2...`;
- tests `4de93001...`;
- final A4 report `47cd9467...`;
- cross-comparison `7c6dab29...`;
- fresh referee `d005e8c3...`; and
- provenance/isolation audit `f4670818...`.

The compact canonical digest remains

`96e527b4eefaf032aeac89ddb960fc2fd4928e3b8204ccbbddbc8fddaa3be631`.

The evidence classifications are unchanged by application except for the
three judge entries described above. Superseded unscoped, stale-hash, and
cache-vulnerable cycles remain negative. The old unscoped A4 report remains
inconclusive only for the legacy diagnostic relation. The original A3
reconstruction is usable only through its scoped domain addendum, and the
compact predecessor audit is usable only for unchanged finite-certificate
predicates under the final scope overlays. Neither supplies current
source-execution or unrestricted-guard authority.

## D20 statement, faces, and source-execution boundary

The realized `SHELL-exact-d20-closure` statement contains the corrected guard

`k_11(rho)>12 for rho_c<=rho<1`.

It records the exact residual

\[
\mathcal D_{20}=\{\rho_c\leq\rho<39/50,\quad
k_{11}(\rho)<K<U(\rho)=K_0(\rho)\}
\]

and the disjoint split into `K<=200` and `K>200`, with `K=200` compact-owned.
The included face `rho=rho_c` remains subject to both strict frequency
inequalities. The faces `rho=39/50`, `K=k_11(rho)`, and `K=U(rho)` remain
outside D20. The `U<200`, `U=200`, and `U>200` cases remain exhaustive, so
the realized successor D21 is empty without omitted or double subtraction.

The certificate/analytic boundary is preserved. Compact computation owns
only its 10,580 finite rectangle leaves; aggregate computation owns only its
1,286 finite base boxes at `K=200`; A3 owns the spectral bridges and universal
derivative, curvature, and two-integration propagation. No finite
certificate is broadened into an all-frequency proof.

## Legacy diagnostic detachment and no higher promotion

After application, none of `SHELL-rho-compact`, `TARGET-shell-d3`, or
`POLYA-program-target` contains `COMP-certified-bessel` as a dependency,
permitted dependency, blocker, or implication. The diagnostic parent no
longer implies `TARGET-shell-d3`.

`COMP-certified-bessel` itself remains `diagnostic_only`; its foundational
dependencies and pilot reverse edges remain intact. Its update consists only
of the intended theorem-link detachment, inconclusive chronology,
falsification protection, next action, and Round 21 provenance. The patch did
not broaden or relabel it.

Empty D21 did not promote the compact assembly, uniformity, theorem, or
program target. The required theorem assembly, endpoint seams, `K=0`,
normalization, scaling, theorem-level clean-room review, fresh theorem
referee, non-tiling work, and program audit remain outstanding.

## Final determination

The actual working-tree graph is canonically serialized and exactly equal to
the independently expected one-time application result after normalizing only
the generated application timestamp. There is no missing operation, extra
operation, repeated application artifact, stale promotion, graph defect, or
provenance mismatch.

**Final verdict: PASS. First issue: none.**
