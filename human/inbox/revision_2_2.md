
# 1. Verdict on rigor
No fatal gap found. The live implication chain — strict phase enumeration → FLPS enclosure → strict lattice proxy → exact defect identity W − P = D_rad − E_ang → ratio-sharp angular blocks + tangent-envelope radial retention + ball quarter-layer + scalar convexity closure (0 < ρ ≤ 39/50), with the two-screen optical theorem (39/50 ≤ ρ < 1) and the no-mode region — is mathematically sound as written, with correct strict-wall bookkeeping throughout. I found one mandatory certificate omission (D1), one unproved displayed inequality (D2, one-line fix), and one inaccurate verification-boundary appendix (D3). All three are repairable without touching the mathematics.

## 1. Theorem-dependency audit (live chain only)
Proved in-manuscript, verified line-by-line:

Prop. 2.1 (unitary decomposition, completeness, compact resolvent): correct, including the form-domain closure via truncation + Fatou and the operator-norm resolvent limit using L_ℓ ≥ ℓ(ℓ+1).

Prop. 2.2 (Bessel determinant, simplicity via Lagrange identity, strict phase count): correct. The Wronskian argument for exactness of the determinant criterion, the zero-energy limit lim D_{ν,ρ} = (ρ^{−ν}−ρ^ν)/(πν) > 0, and phase monotonicity via Nicholson (DLMF 10.9.30, the only DLMF equation actually used live) all check.

Prop. 2.5 (phase-to-lattice): correct on all three regimes (0 ≤ z < μ via the min of the H-bound and the +1/4 correlated bound; μ ≤ z ≤ K; z ≥ K excluded by γ < 1/4). The exact action integral (2.19) reproduces 2(1−ρ³)K³/(9π); I re-derived the two elementary integrals (1/3 and 2/9).

§3 tools: strict product majorant (min–max direction and all walls correct, including the ceiling-at-integer exclusion), action geometry/curvature switch (the δ(y)/y integral identity and g = O(t^{−1/3}) verified), strict layer cake (interchange exact at every wall; Σ_{ℓ<M}(ℓ+½) = M²/2), sawtooth lemma (I reconstructed the full Stieltjes bookkeeping; the discarded terms −∫𝒲 dg and g(τ)(τ/4+3/32) are indeed nonnegative, and the improper boundary terms vanish at the stated O(t^{5/3}) rate).

Supplement (1)–(53): every step re-derived independently. Highlights: the exact defect identity (13); the slope cap −A′ ≤ arccos(ρ)/π (Lemma 1 — the genuinely "ratio-sharp" ingredient, correct on both branches); the disjoint-left-block estimate (15)–(16) giving E_ang < (1−ρ²)K²/6 − N(3π/(8θ) − 1/4) with ∫₀^T R = (1−ρ²)K²/8 verified; the retained remainder (20) with correct interface cancellation; the tangent envelope 0 ≤ L♯ ≤ 𝒲 (equality on [0,¼], difference ½(t−¼)² on [¼,¾]); the substitution (26) ∫₀^τ L♯′ g dt = ∫_{ρK}^K 2z min{G_K, ¼} dz; the truncated layer cake (29) identifying the universal ball quarter-layer B₀(K) (uses τ > 1/4, which is exactly where hypothesis ρ ≤ 39/50, K ≥ π/(1−ρ) enters via τ² ≥ 44/225 > 1/16); the algebraic identity G(ρ, π/e) = F₀(e^{−1/6}) — I expanded it from scratch and it matches (42) exactly, including the cancellations 2ρ²−3ρ+1 = e(2e−1) producing the −π²x⁶/12 + π²/6 terms; and the convexity closure F₀ > 0, D₀ > 0, ∂²_K G > 47447/443682 > 0, whose logic (positive value + positive slope + 
convexity on K ≥ K₀, for each fixed ρ) is airtight.

Optical theorem (§4.2): both screens verified, including continuity of the deficit D/π² across a = mπ with the (N,t)=(m−1,1) assignment, the discriminant argument for monotonicity in N, the minimum at t = C_D, the quarter-circle bound Σbₙ < a²/4, and the inclusive meeting face c/ε ≥ 2252/275 > 22/7 > π.
§5 assembly and dilation, §6 non-tiling: correct. The non-tiling proof (radiality ⟹ translates, ball-separation ⟹ local finiteness, a.e.-coverage reduction, the sphere–plane intersection measure argument, and the coincident-sphere exclusion) is complete; the closed-shell and a.e. variants are handled. The strict↔non-strict conversion (1.4) is clean.

External inputs (verified against source): FLPS-annuli Theorem 5.1 and Lemma 5.2 are quoted exactly, with matching hypotheses: real order z ≥ 0 (so half-integers need no interpolation — confirmed in the published statement), both oscillatory and evanescent regimes, the same H_μ, F_μ definitions, and the two-sided bound (5.8) valid on 0 ≤ z ≤ μ inclusive. These rest on the published FLPS SIAM 2024 phase paper. The attribution is accurate.
Exact-rational verification (all confirmed by exact arithmetic): low-screen reserve = 39569/2772225000; product-deficit minimum = 1822532/91552734375; optical high reserve = 14817541/472867032960000 ≈ 3.13×10⁻⁸; 2252/275 − 22/7 = 9714/1925; cube identities (39); certified bounds (38) from 157/50 < π < 22/7 (e.g. A < 49/40 ⟺ π⁶ < 235298/243, satisfied by (22/7)⁶); F₀(1) ≥ 8269/30000, F₀‴ coefficient = 676141/2450, D₀(1) ≥ 1/300, D₀′(1) ≥ 54/175, D₀″ > 1951/100, ∂²_K G > 47447/443682, τ²-margin 479/3600 — all reproduce exactly as displayed.

Numerical regression (evidence, not premise): at 24 points spanning ρ ∈ {0.05,…,0.97} and K from just above the no-mode wall to 8× it, including wall-adjacent values, the exact cross-product counts satisfy N_D ≤ P < W and W − P > G(ρ,K) > 0 in the ratio-sharp regime, and N_D < W in the optical regime. (A pleasing exact-consistency check: at ρ = 0.9, K = 31.42 the code returns N_D = 1 — precisely the ℓ = 0 mode at k = 10π = 31.4159… just under the threshold.)

# 3. Defects found (none fatal; ordered by severity)

## D1 — Missing π certificate in the optical theorem (mandatory fix). 
The proof of Lemma 4.2 asserts q = 106/333 > 1/π, used four times (bounding εI₀, ε²a/π, qa > 1, and the high-screen E/a²). This is equivalent to π > 333/106, which is true (333/106 is a lower convergent of π) but not derivable from the manuscript's declared bound π > 157/50: exactly, (106/333)·(157/50) − 1 = −4/8325 < 0. The relative margin π/(333/106) − 1 ≈ 2.7×10⁻⁵ is far tighter than 157/50. Under the manuscript's own verification-boundary standard, a certified rational lower bound π > 333/106 must be added as an explicit external input (a classical one-line certificate exists, e.g. an Archimedean/Machin-type or a Dalzell-style integral identity; note Archimedes' 223/71 does not suffice, since 223/71 < 333/106). Nothing else changes: the reserve rationals were computed with this q and remain valid once the certificate is stated.
## D2 — Supplement (24) is asserted without proof. 
The lower turning bound G₁(s) ≥ (2√2/(3π))(1−s)^{3/2} is used twice (τ > 1/4 and the R_B minorant (31)) but only its integral representation is derived. The missing step is the chord bound arccos u ≥ √(2(1−u)) (from θ ≥ 2 sin(θ/2)), integrated. One displayed line fixes it. I flag it only because the manuscript's stated standard is that every step is on the page; note the live main text proves only the reverse-direction bound (Lemma 2.3), so (24) is currently proved nowhere in the live corpus.
## D3 — Appendix A does not match the live dependency set. 
The declared external inputs include Lorch's inequalities (i)–(ii), DLMF (10.21.2) contiguous-order interlacing, DLMF (10.47.3), and the FLPS floor-sum inequalities. None of these is used by the live chain — they belong entirely to the retired routes inside \iffalse (the floor-sum Lemma sits at dead lines 844–899). Conversely the appendix omits the π > 333/106 requirement (D1). The true live external set is: (i) standard SL/spherical-harmonic/Bessel facts + Nicholson (10.9.30); (ii) FLPS-annuli Thm 5.1 & Lem 5.2 (⇐ FLPS-phase 2024); (iii) rational π certificates 157/50 < π < 22/7 and π > 333/106. Overstating inputs is safe-side, but the verification-boundary claim should be exact, and referees will notice.

## Minor items: Lemma 2.3 (upper turning bound) is stated live but never used live — retire or mark archival. 
The module source analytic/ratio-sharp-global-closure.tex and the summary file are \input/\includepdf targets not shipped in the source bundle I received — they must accompany any submission (I audited them via the compiled supplement, which is faithful). Finally, the constants are extremely fragile: the optical high reserve is 3.1×10⁻⁸·a², the low reserve 1.4×10⁻⁵·a², and A < 49/40, C₁ < 13/25 hold with ~0.25% margins. Correct as exact rationals, but I recommend a "fragile constants" table so future edits cannot silently break them.
