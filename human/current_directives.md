# Current Directives

## Authoritative theorem boundary

Preserve the project-internal, purely analytic proof of the sharp Dirichlet
Pólya inequality for every genuine three-dimensional spherical shell
\(A_{r,R}\), \(0<r<R\). The result is not a theorem for general domains and
does not carry a novelty, priority, peer-review, or publication claim.

The general-dimensional shell theorem is a separate active extension and is
**not proved**.  The first-floor no-drop branch has now been closed and
independently integrated; the residual high-floor first-drop branch
(f\ge2, p<n) remains open.

Never transfer the completed (d=3) status to (d\ge4), and never cite
finiteness of a residual chamber family as closure.

## Live proof architecture

Use exactly the following disjoint unit-shell cover:

1. \(K\leq \pi/(1-\rho)\): the strict count is zero.
2. \(K>\pi/(1-\rho)\) and \(0<\rho<39/50\): use the ratio-sharp global
   defect theorem.
3. \(K>\pi/(1-\rho)\) and \(39/50\leq\rho<1\): use the optical theorem.

The shared ratio face \(\rho=39/50\) belongs to the optical owner, and the
frequency face \(K=\pi/(1-\rho)\) belongs to the no-mode owner.

The ratio-sharp theorem must retain:

- the exact strict layer cake and angular-wall convention;
- \(q\leq\arccos(\rho)/\pi\);
- the tangent envelope \(L_\sharp\leq\mathfrak W\);
- the scope \(\tau>1/4\) before invoking the \(g(\tau)/8\) reduction;
- nonnegativity of the ball-inverse lower bound before squaring; and
- the displayed exact rational convexity margins.

The former \(\rho_*\) split, \(k_6,\ldots,k_{11}\), 38-state theorem,
\(\mathcal D_{16},\ldots,\mathcal D_{20}\), 611-row ledger, interval
certificates, and executable event replays are historical or optional
regression evidence only. They must not be cited as premises of the revised
proof.

## Verification standard

After a mathematical source edit:

1. rebuild the main paper, ratio-sharp module, and supplement;
2. run graph validation and the ratio-sharp exact regression tests;
3. scan the live TeX for missing references and retired dependencies;
4. inspect rendered title, theorem, global-closure, final-assembly, and support
   pages; and
5. synchronize release PDFs and hashes.

Computation may check transcription and exact arithmetic, but it is not a
premise of the analytic theorem.

## Active general-dimensional strategy

Use `human/inbox/general-d_simplified_analytic_strategy.md` as the steering
document for the extension.  Preserve the exact phase proxy, harmonic
branching, signed primitive, and master identity

\[
 W_d-P_d^{<}=\mathcal B_d(A)+\sum_m c_{d,m}D_A(r_m).
\]

Freeze the existing ratio ladder and certificate campaign.  Round 10 has
proved and independently audited the fractional terminal reserve.  Round 11
has proved, and a clean-room audit has checked, the (B=0) first-floor corner.
Round 13 gives an exact extension-grid counterexample to the lossy Round
11--12 pre-shelf sign target; the exact defect stays positive there.  Round 14
restores the exact no-drop head and exact inner cap.  Round 15 restores the
exact terminal count and proves the lossless endpoint identity.  Round 16
reduces the endpoint to \(F(K,a)\ge1/2\), proves strict radial monotonicity,
and closes the suspected \(q=3\) double face.  Round 17 proves the exact
action-face quantile reduction.  Round 18 proves the simultaneous-wall value
symbolically, and Round 19 proves the entire action-face derivative through a
one-radius Jensen reduction.  The active order is now:

1. keep the now-audited WP2 first-floor theorem frozen.  Its integration
   audit covers the exact head, exact cap, exact terminal count, \(\chi_q\),
   strict inverse convention, owner activity wall, both parity grids, and the
   exact proof that the apparent \(z=2\) boundary is redundant;
2. work only on the exact remainder-poor high-floor sector isolated in Round
   27.  Round 22 proves \(\Phi_\delta>\mathcal R_J\), with
   \(\mathcal R_J=\mathcal R+1/7-2I_\mu(q)\), and Rounds 23--26 provide valid
   curvature-cell, coupled-owner, stationary, and lossless projection tools.
   Their proposed global signs are now all rejected: Round 26 gives
   \(\mathcal F<-1/50\) and \(\mathcal C_8<-7\) at exact-domain witnesses;
   Round 27 gives an exact witness with
   \(\mathcal C_{\max,8}<-4/3\) and \(\mathcal R_J<-6/5\), while
   \(\mathscr S>47\) and \(\Phi_\delta>40\).  Do not try to repair those
   global lower scalars.  Instead put
   \(d=1-2t/\pi\), \(E=e_0+e_p\), \(\Delta=e_0-e_p\), and
   \(E_*=(p-dm)/(2p)\).  Freeze the proved automatic branch
   \(p\le dm\) or \(E\ge E_*\), and prove on
   \[
    p>dm,\qquad 0\le E<E_*<\frac12
   \]
   the exact residual
   \[
    \max\{0,L_T\}+a_p\Delta\ge p(E_*-E).
   \]
   Round 28 proves the sharper primary projection
   \[
    D_A(r)\ge\Phi_\delta^+\ge
    \Psi^{L_T}_{4,E}
    =\max\{0,L_T\}
     +a_p\max\!\left\{
       \frac{s-1}{s+1}(E+2\lambda),\mathcal K_4
      \right\}
     +p(E-E_*).
   \]
   Round 29 proves its strict increase on every fixed-\(\alpha\) literal
   terminal cell and strict decrease on every fixed-\(Q\), constant-\(K\)
   inverse/outer-\(B\) wall segment.  It also closes the entire primary
   \((r,p,m,f)=(1,2,2,2)\), \(B=Q=1\), \(2<y_1<3\) component with
   \[
    \Psi^{L_T}_{4,E}>\frac{371}{15840}+2\eta_1+2E>0.
   \]
   Round 30 makes activity automatic on the whole \(B=Q=1\), \(2<y_1<3\)
   band, proves the included-shelf terminal term is strictly increasing and
   exceeds \(7/10\), and reduces that shelf to the retained-\(E\) scalar
   \(F(q,r,p,m)\) under three exact necessary conditions.  It also removes
   noncorner lower-\(Q\) endpoints from every \(y_1>1\) band and closes the
   \(\alpha=0\), \(f=2\), \(4/5\le t<13/10\) range.  Round 31 proves that
   the dependent profile \(p\mapsto\mathfrak F(q,q-m-p,p,m)\) is strictly
   convex, excludes the exact
   necessary shelf/hard domain for every real \(q\ge1000\), and reduces a
   weaker finite lower scalar \(L_q\) to three convex edges.  Round 32 proves
   \(m>(6/5)(\pi/\psi-3)\) for \(q\ge35\), truncates the false \(m=1\)
   edge, and closes the exact \(q<1000\) remainder with one directed-Arb
   certificate; the included retained-\(E\) shelf is now closed.  Round 33
   proves that \(H=E-E_*\) is strictly decreasing along every feasible open
   lower-\(Q\) hard-arc segment.  Thus a hard component persists to the lower
   shelf, an activity wall, or \(\alpha\to1^-\); this is not monotonicity of
   the complete \(\Psi\) and does not close those exits.  Round 34 removes the
   activity wall globally on the high-floor first shelf with exact margin
   \(>1/16\).  Its adjacent-action lemma and matched quadratures reduce the
   lower-\(Q\) hard sector to an intrinsic face and stationary alternative.
   The small-phase adjacent-action theorem and Round 35 close every phase of
   that lower-\(Q\) sector analytically.  Treat it as closed.  Round 36 proves
   \(B-Q\in\{0,1\}\), \(L_T>1-J>6/7\) on the gap branch, and full
   positivity there for \(p-dm\le12/7\).  Round 37 proves
   \(B_0=Q=B-1\ge1\), removes the top interval, and derives the
   selected-projection-exact \(\Gamma_{\rm gap}\) normal form and strict
   sufficient gate \(\Phi_\delta^+>\mathcal H_\Delta\).  Round 38 sharpens
   the gap position to \(0<u<\alpha/2<1/2\), proves
   \(B_0(\pi/(2t)-1)>1/5\), and closes \(p-dm\le74/35\), including every
   residual \(p=2\) gap.  It also proves the unified
   \(\chi\)-compensation (R38.20), without signing every remaining
   \(p\ge3\) endpoint.  Round 39 proves \(q\ge5\), \(J<1/10\), and
   \[
    \Gamma_{\rm gap}>
    \frac{11}{10}-\frac{p-dm}{2}.
   \]
   Hence \(p-dm\le11/5\) is closed, and every residual \(p=3\) gap has
   \(dm<4/5\).  On the gap-side outer-\(B\) wall, retain the exact intrinsic
   selected-scalar inequality and its root-free projection.  The simultaneous
   outer-\(B\)/lower-shelf endpoint is now closed: its floor geometry forces
   \(j\ge1\), and the phase-stretch lemma supplies the remaining sign.
   Round 40 proves on the one-sided upper-\(\alpha\) outer-\(B\) endpoint
   that strict midpoint convexity restores the full radial drop and reduces
   the selected scalar to one continuous target
   \[
    \mathcal R_*(p,m,t)>0,
    \qquad
    \mu_*=\max\left\{p+m+2,\frac5{4L_0(t)}\right\}.
   \]
   Both analytic branches are strictly convex in one auxiliary variable.
   Round 41 completes a global rational enclosure and an exact fixed-cell
   Bernstein replay, but its 4,405 load-bearing coefficient signs constitute
   a new computer-assisted certificate.  The revised strategy does not
   authorize that certificate, so the final sign and endpoint remain open.
   Treat the selected \(\Gamma_{\rm OB}\to\mathcal R_*\) route as
   analytically exhausted.  Round 42 has now made the last stronger exact
   Gate-A specialization.  With \(H=(p+a_p)R_1\), it proves structurally
   \[
    \Phi_\delta^+>\mathcal T_{42}:=
    \frac9{10}+B_0\min\{\zeta,H\}+Hh-\frac{p-dm}{2}.
   \]
   Preserve the exact loss ledger
   \[
    \Phi_\delta^+-\mathcal T_{42}
    =\mathcal A_{\rm cap}+\Omega_-+\mathcal A_{\rm adj}
     +B_0(\zeta-M)+jH+(H+2p)e_p,\qquad M=\min\{\zeta,H\},
   \]
   as structural evidence only.  The independent verdict is STRUCTURAL PASS
   only; final sign open.  Round 43
   (`human/outbox/general-d-round-43-hard-remainder-isolation-and-gate-a-stop.md`)
   proves the sharp radical envelope \(H\ge\underline H\), the strict cap
   envelope \(h>U_q^3/(3\pi\mu^2)\), and
   \((E_f-E_*)'(t)>0\) on every fixed-\(f\) chart.  Its relaxed asymptotic
   family proves only that count--phase/radial data cannot sign
   \(\mathcal T_{42}\); its finite negative diagnostic satisfies the ordinary
   shelf and terminal crossing but fails the exact hard owner precisely at
   \(E<E_*\).  A global proof would require the forbidden \(f/B/j\)-indexed
   wall analysis.  The independent audit therefore gives analytic structural
   PASS and **Gate A STOP**.  Gate B is active: restore
   \[
    \mathscr S=D_A(q)+R_p+\frac{dm}{2}
   \]
   with the exact first-shelf trapezoid, terminal surplus, cap, and inverse
   fractions.  Do not repair \(\mathcal T_{42}\) by a count, floor, ratio,
   chamber, or certificate partition, and do not replace it with the rejected
   floor-free adjacent projection.  Then treat the higher
   equal-count \(Q_N\)/inverse
   faces, simultaneous \(\alpha=0\) \(B/Q\) corners, and general lower
   shelves outside the exact Round 32 and Round 39 scopes.
   Retain every inverse fraction, exact cap,
   top interval, actual \(E\), and paired-shelf relation.  Global
   \(\alpha\)-monotonicity is diagnostically false.  The root-free and
   conditional \(\mathcal C_{\max,8}\) routes are auxiliary only.  All Round
   22 and Round 26--33 witnesses are mandatory regression gates;
3. if pointwise CST fails, use the weighted aggregate and the positive
   branching bonus \(\mathcal B_d(A)\);
4. use at most one compact, reproducible certificate in the final implication
   chain.  Since Round 32 now uses the selected compact finite certificate,
   replace the legacy first-floor certificate analytically or consolidate it
   into the same verification artifact before assembly.

Every proposed lemma must print its statement, dependencies, proof,
equality-face audit, loss ledger, falsification check, and exact failure
inequality.  Do not introduce another ratio partition.

Mathematica 15.0 is available for high-precision falsification and symbolic
checks.  Such output is diagnostic unless a separate directed-rounding
certificate obligation is created.

## Next work

For the completed (d=3) theorem, the next required work remains independent
human reconstruction, conventional referee review, and a current
literature/novelty search.

For the active general-dimensional extension, the Round 17 chord scalar is
no longer selected: Round 19 bypasses it and proves the exact derivative.
The selected obligation remains the single high-floor first-drop CST theorem.
Round 27 changes its working domain, Round 28 changes its primary scalar,
Round 29 removes all literal-cell and constant-\(K\) wall interiors while
closing the primary inverse component, Round 30 reduces the first
nonconstant-\(K\) shelf/Q/\(\alpha=0\) endpoint package, Round 31 proves
strict retained-shelf \(p\)-convexity and the exact \(q\ge1000\) exclusion,
 Round 32 closes the remaining included shelf, Round 33 localizes every
 hypothetical smooth lower-\(Q\) hard component, Round 34 proves activity
 cannot be an exit and compresses the remaining lower-\(Q\) question to one
 intrinsic face, and Round 35 closes that face in all phases.
 Round 36 reduces every terminal count to \(B=Q\) or \(B=Q+1\), closes
 the latter when \(p-dm\le12/7\), and gives the exact remaining collision
 map.  Round 37 synchronizes \(B_0=Q=B-1\ge1\) on the residual gap and
 gives one selected-projection-exact normal form plus one count-free
 sufficient gate.  Round 38 closes \(p-dm\le74/35\), confines the remaining
 gap to \(p\ge3\), and supplies the unified \(\chi\)-compensation.  Round 39
 sharpens the live threshold to \(p-dm\le11/5\), derives the exact intrinsic
 outer-\(B\) inequality and its root-free projection, and closes the full
 outer-\(B\)/lower-shelf endpoint without a count or ratio split.  Round 40
 restores the exact shelf drop by midpoint convexity, eliminates \(\mu\)
 monotonically, and leaves the sign \(\mathcal R_*(p,m,t)>0\) in two
 convex representations.  Round 41 gives exact rational enclosures and a
 successful but unauthorized 4,405-sign candidate; its selected projection is
 exhausted.  Round 42 makes the last stronger specialization and proves
 \[
  \Phi_\delta^+>\mathcal T_{42}
  =\frac9{10}+B_0\min\{\zeta,H\}+Hh-\frac{p-dm}{2},
  \qquad H=(p+a_p)R_1,
 \]
 with its exact nonnegative loss ledger and a STRUCTURAL-PASS-only independent
 audit, but not \(\mathcal T_{42}\ge0\).  Round 43 then proves the radical and
 cap envelopes, a relaxed asymptotic obstruction, and the fixed-chart
 derivative \((E_f-E_*)'(t)>0\).  Its finite negative diagnostic fails only
 the exact hard inequality \(E<E_*\).  Since continuing across the sawtooth
 requires a forbidden \(f\)-, \(B\)-, or \(j\)-indexed wall family, Gate A has
 stopped.  No count-by-count, ratio-chamber, or new-certificate repair is
 authorized.  Gate B is active with
 \(\mathscr S=D_A(q)+R_p+dm/2\); retain all exact shelf, terminal, cap, and
 inverse-fraction correlations.  The endpoint, CST, and theorem remain open.
The exact lower bound
\[
 \Phi_\delta^+
 =\max\{0,L_T\}+a_p\Delta+p(E-\frac12)+\frac{dm}{2}
\]
proves the entire remainder-rich branch automatically.  Only
\(p>dm\), \(0\le E<E_*<1/2\) remains, with the residual printed above.
The Round 27 negative \(\mathcal R_J\) witness is remainder-rich and therefore
is not a counterexample to \(\Phi_\delta\), CST, or the theorem.  It proves
that global \(\mathcal R_J\ge0\) and global
\(\mathcal C_{\max,8}\ge0\), like the earlier \(\mathcal R\),
\(\mathcal F\), and \(\mathcal C_8\) signs, are not live proof premises.
None of the earlier reductions authorizes global \(\alpha\)-monotonicity or
lower-wall dominance.  Round 28 proves
\[
 \Phi_\delta^+\ge\Psi^{L_T}_{4,E}\ge\Psi^{\rm rf}_{4,E}
\]
and the one-hard-interval phase lemma.  Keep the actual owner, exact
remainders, literal maximum, every inverse fraction, and exact cap throughout
the residual sector.  Round 29 further proves the exact phase-cell and
constant-\(K\) wall monotonicities and the primary-cell margin above.  The new
integer and half-integer grids start at \(r=1\) and \(r=3/2\); do not extend
the constant-\(K\) hard-gap proof to \(r=0\) or \(r=1/2\).  The Round 30
finite scan through \(q=1000\), including the observed cutoff at \(q=33\),
and the unarchived Round 31 edge experiment remain diagnostic only; the
Round 32 analytic bootstrap and directed certificate are the proof
dependencies.  If the surviving nonconstant-\(K\) endpoint route fails,
return to \(\mathscr S\) and switch
directly to the weighted aggregate rather than introducing another surrogate
or ratio partition.
Do not rewrite `manuscript/spherical-shell-polya-general-d.tex` until the
high-floor gate, backbone audit, and final whole-cover assembly close, or the
proof completes the weighted aggregate route instead.
