# General dimension, Round 48: portfolio discovery and bounded certification

## Verdict

**INCOMPLETE — SUBSTANTIVE LOCALIZATION.**

Round 48 neither proves nor falsifies the literal four-dimensional aggregate

\[
 \mathrm{WT}_4=\frac{K^4-\mu^4}{64}
 -\sum_{1\le a<K}a^2[A(a)+1/4]_<\ge0
\]

on the complete strict-active domain.  It therefore does not close the
general-dimensional Gate B target or the all-dimensional theorem.

It does certify two new bounded lemmas:

1. every complete inverse-action cell wholly in the outer branch is strictly
   positive, with explicit reserve (19N/48+27/128);
2. every complete deep-inner cell with
   (n-1\ge A(\mu/\sqrt2)) is strictly positive.

The completed three-dimensional theorem remains frozen.

## Lossless inverse-layer ledger

With (H=A(0)), decreasing inverse \(\xi=A^{-1}), and

\[
 F(t)=\frac{\xi(t)^3}{3},\qquad
 N_n=\lceil\xi(n-1/4)\rceil-1,
\]

Round 48 uses the exact identities

\[
 W_4=\int_0^HF(t)\,dt,
 \qquad
 P_4^<=\sum_{n-1/4<H}S_2(N_n).
\]

Thus every complete cell has residual

\[
 L_n=\int_{n-1}^nF(t)\,dt-S_2(N_n).
\]

This QCL cell decomposition is a sufficient route to the literal aggregate;
it is not asserted to be equivalent cell by cell.

## Certified regions

For a full outer cell (n\le A(\mu)), the lead proof compares the exact
past/future kernel (J=-F'=\xi^2/h(\xi)), while the clean-room proof uses a
convex quantile transport.  A distinct adversarial audit reconstructed all
inequalities and strict-wall cases.  The result is

\[
 L_n>\frac{19N_n}{48}+\frac{27}{128}>0.
\]

For a complete deep-inner cell (n-1\ge A(\mu/\sqrt2)), the lead uses the
shell-mixture identity

\[
 2s_R(z)-zs_R'(z)=
 \frac{z(R^2-2z^2)}{\pi R(R^2-z^2)^{3/2}},
\]

and the clean-room proof independently uses monotonicity of

\[
 \phi(u)=2\arcsin u-\frac{u}{\sqrt{1-u^2}}.
\]

Both give convexity of (F) on the cell and then (L_n>0).  A third
adversarial reconstruction passed the packet boundaries.

These are mathematical lemmas, not computational certificates.  The
outer-cell and deep-inner Mathematica replays are symbolic sanity checks,
not proof premises.

## Exact rejected strengthening

The unrestricted continuous quarter-node replacement

\[
 p(\xi(n-1/4))\le\int_{n-1}^{n}F(t)\,dt
\]

is false.  At an exact rational interface-straddling cell, directed Arb
evaluation gives continuous margin

\[
 -572.69422630\pm5.18\times10^{-9}<0.
\]

The discrete QCL margin at the same point is

\[
 956715.736090048\pm8.69\times10^{-10}>0.
\]

The initial discovery output mistranscribed the moment primitive.  The final
script uses the corrected primitive, whose derivative Mathematica verifies
exactly, and still certifies the negative continuous margin.  Hence the
negative result is retained with corrected provenance.

## Portfolio synthesis

- Direct inverse layers are the most localized current route.
- Pointwise (D_A(a)) retains an exact sawtooth identity, but its linear
  kernel cannot be substituted for the weighted QCL kernel.
- The Round 47 component-(U) route remains active but is not the sole route.
- The row-cone reserve (\Theta) has an exact adverse-wall reduction and no
  found negative, but is a stronger sufficient claim and remains open.
- Pure total positivity for arbitrary atomic measures is rejected; the
  uniform shell interval must be used.
- Action-flow wall geometry is exact, but tangential derivatives have mixed
  signs.
- No exact literal \(\mathrm{WT}_4\), QCL, or (\Theta) counterexample was
  found.  Finite positive searches remain diagnostic.

## First remaining gap

The inverse-layer route is now confined to:

1. complete shallow-inner cells with heights between (A(\mu)) and
   (A(\mu/\sqrt2));
2. the possible interface-straddling cell;
3. the terminal truncated cell; and
4. exact compensation between those residuals and the unused top interval.

A Round 49 continuation should attack this exact residual ledger, while
retaining the component and row-cone routes for independent falsification or
compensation.  It must not restore the false continuous quarter-node claim.

## State Patch

The validated graph update is conservative:

- `SHELL-general-d-weighted-aggregate` remains the separate all-dimensional
  `proposed` route and retains its theorem-level sufficient-route edge;
- `SHELL-general-d-weighted-aggregate-d4` is created as the scoped literal
  Round 48 target and remains `proposed`, with no theorem-level implication;
- `SHELL-general-d-d4-full-outer-action-cell-round48` is created as
  `proved_internal` after lead, clean-room, and adversarial PASS;
- `SHELL-general-d-d4-deep-inner-action-cell-round48` is created as
  `proved_internal` after lead, clean-room, and adversarial PASS;
- the general-dimensional theorem remains unproved; and
- no status of the completed three-dimensional chain changes.

The graph validator reports PASS after these changes.
